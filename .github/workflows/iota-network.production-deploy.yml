name: Production Deploy for the IOTA Network website

on:
    push:
        branches:
            - main
    workflow_dispatch:

jobs:
    deploy-production:
        uses: ./.github/workflows/monorepo-app-vercel-deploy.yml
        with:
            PRODUCTION: true
            APP_NAME: IOTA Network
            PACKAGE: iota-network
        secrets: inherit
