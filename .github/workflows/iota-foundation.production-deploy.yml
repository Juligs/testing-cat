name: Production Deploy for the IOTA Foundation website

on:
    push:
        branches:
            - main
    workflow_dispatch:

jobs:
    deploy-production:
        uses: ./.github/workflows/monorepo-app-vercel-deploy.yml
        with:
            PRODUCTION: true
            APP_ID: iota-foundation
        secrets: inherit
