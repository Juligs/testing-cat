# `icons`

## Exporting Icons

1. Clean the project by running `pnpm clean:all` from the icons folder.
1. Open Figma Icons file. Create a mass export using `cmd` + `shift` + `e`. Save the SVGs to `icons/svgs`.
1. Run `pnpm generate` to generate the new icon library.
1. Commit the changes and submit a PR.
