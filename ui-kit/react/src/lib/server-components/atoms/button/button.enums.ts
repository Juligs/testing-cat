export enum ButtonSize {
    Small = 'small',
    Medium = 'medium',
    Large = 'large',
}

export enum ButtonVariant {
    Primary = 'btn-primary',
    Secondary = 'btn-secondary',
    Ghost = 'btn-ghost',
}

export enum ButtonRadius {
    Default = 'default',
    Rounded = 'rounded',
}
