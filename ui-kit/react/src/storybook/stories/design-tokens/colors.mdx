import { Meta, ColorPalette, ColorItem } from '@storybook/blocks';
import {
    IOTALABS_COLOR_PALETTE,
    NETWORK_COLOR_PALETTE,
    FOUNDATION_COLOR_PALETTE,
} from '../../../lib/constants/colors.constants';
import { transparencyColorPalette } from '../../utils';

<Meta title="Design Tokens/Colors" />

## LABS

<ColorPalette>
    {Object.entries(IOTALABS_COLOR_PALETTE).map(([color, values]) => {
        return <ColorItem key={color} title={color} colors={values} />;
    })}
</ColorPalette>

## NETWORK

<ColorPalette>
    {Object.entries(NETWORK_COLOR_PALETTE).map(([color, values]) => {
        return <ColorItem key={color} title={color} colors={values} />;
    })}
</ColorPalette>

## FOUNDATION

<ColorPalette>
    {Object.entries(FOUNDATION_COLOR_PALETTE).map(([color, values]) => {
        return <ColorItem key={color} title={color} colors={values} />;
    })}
</ColorPalette>

## TRANSPARENCY

<ColorPalette>
    {Object.entries(transparencyColorPalette).map(([color, values]) => {
        return <ColorItem key={color} title={color} colors={values} />;
    })}
</ColorPalette>
