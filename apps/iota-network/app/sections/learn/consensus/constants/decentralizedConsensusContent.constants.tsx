import { TitleTextSize, VerticalTitle } from 'react-ui-kit';

export const DECENTRALIZED_CONSENSUS_VERTICAL_TITLE: React.ComponentProps<typeof VerticalTitle> = {
    title: 'Real networks. Real resilience.',
    subtitle: 'Starfish ensures steady progress, whatever the conditions',
    body: 'A starfish can regenerate from a single arm. Similarly, Starfish reconstructs transaction data from protocol shards – so the network keeps moving, even when parts fall behind.',
    size: TitleTextSize.ExtraSmall,
};
