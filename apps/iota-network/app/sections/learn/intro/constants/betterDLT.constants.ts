import { TitleTextSize } from 'react-ui-kit';

export const BETTER_DLT_VERTICAL_TITLE_CONTENT = {
    title: 'Building a Better DLT?',
    subtitle: 'The next generation of blockchain',
    body: 'IOTA fundamentally differs from traditional blockchains. Instead of relying on linear blocks processed at fixed intervals, IOTA uses a unique data structure that enables interaction at any point in time, allowing operations to occur in parallel.',
    size: TitleTextSize.Small,
};
